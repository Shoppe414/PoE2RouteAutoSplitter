v0.4.5-structure-first development validation
---------------------------------------------
Static source/package validation passed for the structure-first multilingual pause detector. No Windows/.NET compile was performed here. Verified source lexical structure, structure/banner/text evidence weights (68/24/4/4), non-English skipping of English Resume/Exit text searches, JSON integrity, and shared game-language propagation. The new masked structure score requires live threshold calibration across languages/resolutions.

PoE2 GameTimeWatcher v0.4.4 - Shared Settings Static Validation
=================================================================

Development validation date: 2026-08-17

New v0.4.4 checks:
- `--settings <path>` reads the GameTimeWatcher section of poe2_run_settings.json.
- User-facing overrides cover ProvisionalTimeoutMs and the five pause/MTX template thresholds.
- Default values remain the v0.4.3 tested values.
- Invalid/missing shared settings fall back to the validated component config.json.
- Run-Diagnostic.ps1 accepts -SettingsFile, includes its SHA-256 in the diagnostic summary, passes it to the watcher, and copies the snapshot into the diagnostic folder.
- No Windows/.NET compilation was attempted in this environment, per project test workflow; rebuild and live validation are performed by the user on Windows.

--- Prior validation baseline follows ---

PoE2 GameTimeWatcher v0.4.3 source validation

- State protocol v2: RUNNING / PAUSED / PENDING_PAUSE / PENDING_RUN.
- ESC/controller Start is polled on an independent 5 ms thread and can publish a provisional state immediately.
- Provisional state includes the input-edge `originUtcTicks` and a monotonic `stateSequence`.
- Screen matching remains final authority; rejected provisional states resolve to the last stable visual state.
- Default provisional timeout: 1200 ms.
- Confirmed state publication occurs before state/menu diagnostic PNG encoding.
- Mouse-driven exits from Pause Menu publish PENDING_RUN on the first visual Running frame.
- MTX remains visually authoritative and is not toggled directly by ESC.
- No Path of Exile 2 process-memory reads are used.
- Windows/.NET compilation and LiveSplit runtime validation are still required.

PoE2 GameTimeWatcher v0.4.1 - Static Validation Summary

Scope
- Optional manual-pause helper only.
- Loading-screen Game Time remains ASL/Client.txt-owned.
- No Path of Exile process-memory reads.
- No LiveSplit Pause hotkey injection or rebinding.

v0.4.1 latency changes
- Pause menu template searches use narrow canonical anchor windows rather than broad upper-screen scans.
- Known canonical anchors retain conservative vertical/horizontal tolerance and all prior scale variants.
- ESC/controller Start is polled on a dedicated 5 ms background thread, independent of visual analysis.
- Input remains an acceleration hint only; visual confirmation remains authoritative.
- v0.4.0 heartbeat/state-writer isolation, content-bound handling, MTX detection, and fail-open behavior are retained.

Offline anchor validation
- 2048x576 GameTimeWatcher pause probe: stack~0.989, resume~0.913, banner~0.998, exit~0.790 remain inside the new local search windows.
- Independent wide pause screenshot canonicalized to 576px: stack~0.931, resume~0.893, banner~0.981, exit~0.946 remain inside the new local search windows.

Config defaults
- CaptureFps=10
- FastCaptureFps=30
- InputFastModeMs=1500
- InputHintWindowMs=750
- HeartbeatMs=250
- ConfirmPausedFrames=2
- ConfirmRunningFrames=2
- PauseStackThreshold=0.62
- MtxShopThreshold=0.70

Environment limitation
- Windows .NET SDK/runtime compilation cannot be executed in this container.
- Local Windows build + LiveSplit/PoE2 test is required.

Integration note: matching ASL protocol v2.1 is required for provisional timestamp accounting.

Full-package integration static validation: 138 / 138 checks passed.

